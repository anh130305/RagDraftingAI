================================================================
PROJECT CONTEXT — RAG Soạn thảo Văn bản Hành chính Việt Nam
================================================================
Cập nhật lần cuối: 2025-04 (sau khi hoàn thiện hybridRetrieval v3 — final)

## TRẠNG THÁI TỔNG THỂ
  ✅ checkData.ipynb          — Khám phá & kiểm tra 3 dataset gốc
  ✅ preprocessData.ipynb     — Làm sạch, chuẩn hoá, tạo doc_id
  ✅ filterData.ipynb         — Lọc legal dataset xuống ~110k hàng (mới)
  ✅ chunkData.ipynb          — Chunking ~165k chunks (đã cập nhật input path)
  ✅ embedAndIndex.ipynb      — Build ChromaDB trên Kaggle GPU T4
  ✅ hybridRetrieval_v3.ipynb — BM25 bigram + Dense + RRF + Reranker + Chunk Expand (v3, final)
  □  RAG generation pipeline  — Chưa thực hiện
  □  Prompt templates         — Chưa thực hiện
  □  Evaluation               — Chưa thực hiện
  □  API / UI                 — Chưa thực hiện


## MỤC TIÊU DỰ ÁN
Xây dựng hệ thống RAG (Retrieval-Augmented Generation) để hỗ trợ soạn
thảo văn bản hành chính Việt Nam, gồm 3 chức năng chính:
  1. Truy xuất điều khoản pháp luật liên quan (legal retrieval)
  2. Gợi ý biểu mẫu hành chính phù hợp (form retrieval)
  3. Few-shot retrieval ví dụ văn bản đã soạn sẵn (example retrieval)


================================================================
1. CÁC DATASET GỐC (Raw)
================================================================

--- legal_dataset ---
File    : dataset/raw/legal_dataset.parquet
Số hàng : 515,188
Mỗi hàng: 1 Điều khoản của 1 văn bản pháp luật
Cột     : id, ministry, type, name, chapter_id, chapter_name, article, content
Ghi chú :
  - "id" là số hiệu văn bản (VD: "30/2020/NĐ-CP") — KHÔNG phải row key
    vì nhiều điều khoản cùng văn bản chia sẻ 1 id (95% trùng)
  - 17.61% content = null
  - 6.29% content trùng lặp
  - Cột "type" có 98 giá trị unique do bị ngắt dòng, nhiễu
  - 8 văn bản pháp luật cốt lõi cho RAG hành chính đều có mặt đầy đủ
    (NĐ 30/2020, Bộ luật LĐ, Luật CBCC, Luật VC, Luật BHVBQPPL, v.v.)

--- forms_dataset ---
File    : dataset/raw/forms_dataset.parquet
Số hàng : 10 (10 biểu mẫu hành chính)
Cột     : form_id, form_type, purpose, required_fields, template_markdown
Ghi chú :
  - "required_fields" kiểu numpy.ndarray (list các placeholder)
  - "template_markdown" chứa template với {{FIELD_NAME}} placeholders
  - Trung bình 12.4 required_fields/form, min=9, max=14
  - Dataset sạch, 0 missing, 0 duplicate

--- forms_examples_dataset ---
File    : dataset/raw/forms_examples_dataset.parquet
Số hàng : 150 (15 ví dụ × 10 forms)
Cột     : form_id, form_type, example_id, scenario, fields, filled_content, doc_id
Ghi chú :
  - "fields" kiểu dict (Python), chứa giá trị điền vào từng placeholder
  - "filled_content" là văn bản đã điền sẵn (không còn {{}} nào)
  - 150 unique doc_id, phân bố đều 15/form
  - Dataset sạch hoàn toàn


================================================================
2. PREPROCESSING — preprocessData.ipynb  ✅ HOÀN THÀNH
================================================================

Input : dataset/raw/*.parquet
Output: dataset/processed/*_processed.parquet

--- legal_dataset_processed ---
File    : dataset/processed/legal_dataset_processed.parquet
Số hàng : 392,079  (giảm từ 515,188 — loại 23.9%)
Cột     : doc_id, id, ministry, type_normalized, name,
           chapter_id, chapter_name, article, content
Các bước cleaning:
  1. Drop content = null        → loại 90,706 hàng (17.6%)
  2. Drop exact duplicate       → loại 638 hàng (0.2%)
  3. Drop content duplicate     → loại 31,765 hàng (7.5%)
  4. Normalize type → 9 nhóm chuẩn:
       THÔNG TƯ (33.8%), QUYẾT ĐỊNH (21.9%), NGHỊ ĐỊNH (18.2%),
       NGHỊ QUYẾT (15.3%), LUẬT (7.9%), PHÁP LỆNH (1.7%),
       KHÁC (1.2%), VĂN BẢN KHÁC (0.0%), KHÔNG RÕ (0.0%)
  5. Normalize text (whitespace, newline, tab)
  6. Tạo doc_id = SHA256(số_hiệu + article + content_hash[:10])
     → 392,079 unique doc_id, 0 duplicate
Kết quả: 0 missing content, 0 duplicate doc_id, 9 nhóm type chuẩn

--- forms_dataset_processed ---
File    : dataset/processed/forms_dataset_processed.parquet
Số hàng : 10
Cột     : doc_id, form_id, form_type, purpose, required_fields, template_markdown
Các bước: normalize text, convert required_fields numpy→list, doc_id = form_id
Kết quả: 0 missing, 0 duplicate

--- forms_examples_dataset_processed ---
File    : dataset/processed/forms_examples_dataset_processed.parquet
Số hàng : 150
Cột     : doc_id, form_id, form_type, example_id, scenario, fields_json, filled_content
Các bước: normalize text, serialize fields dict → fields_json (JSON string)
Kết quả: 0 missing, 0 placeholder chưa điền, 0 duplicate doc_id


================================================================
2.5. FILTER DATA — filterData.ipynb  ✅ HOÀN THÀNH (mới thêm)
================================================================

Mục tiêu: Giảm dataset legal từ ~392k xuống ~169k hàng để giảm tải
          cho máy local (16GB RAM, CPU-only). Tổng hệ thống từ ~11.5GB
          xuống ~5.4GB.

Input : dataset/processed/legal_dataset_processed.parquet (392,079 hàng)
Output: dataset/processed/legal_dataset_filtered.parquet  (168,946 hàng)

Chiến lược filter:
  GIỮ LẠI theo type_normalized:
    - LUẬT        (7.9%)  — quy phạm cao nhất           →  31,145 hàng
    - NGHỊ ĐỊNH   (18.2%) — hướng dẫn thi hành luật     →  71,176 hàng
    - PHÁP LỆNH   (1.7%)  — có giá trị như luật         →   6,798 hàng
    - NGHỊ QUYẾT  (15.3%) — chính sách cấp cao          →  59,827 hàng
  GIỮ LẠI bổ sung theo số hiệu (dù thuộc type nào):
    - 30/2020/NĐ-CP  (Công tác văn thư)
    - 01/2011/QH13   (Luật Lưu trữ)
  LOẠI BỎ:
    - THÔNG TƯ    (33.8%) — hướng dẫn kỹ thuật chuyên ngành
    - QUYẾT ĐỊNH  (21.9%) — quyết định hành chính cụ thể từng vụ việc
    - KHÁC, VĂN BẢN KHÁC, KHÔNG RÕ

Kết quả thực tế (đã chạy):
  - Số hàng  : 168,946 (43.1% so với processed, giảm 56.9%)
  - Dung lượng file: 109.8 MB
  - Coverage : 8/8 văn bản cốt lõi vẫn đầy đủ (đã verify)

⚠️  Lưu ý: NGHỊ QUYẾT (59,827 hàng) không có trong ước tính ban đầu
    nên số hàng thực tế (~169k) cao hơn ước tính (~110k).

⚠️  forms_dataset_processed và forms_examples_dataset_processed
    KHÔNG bị ảnh hưởng — giữ nguyên 10 và 150 hàng.


================================================================
3. CHUNKING — chunkData.ipynb  ✅ HOÀN THÀNH (đã cập nhật)
================================================================

Input : dataset/processed/legal_dataset_filtered.parquet  ← ĐÃ ĐỔI
        dataset/processed/forms_dataset_processed.parquet
        dataset/processed/forms_examples_dataset_processed.parquet
Output: dataset/chunks/*.parquet + *.jsonl

Tham số chunking:
  LEGAL_MAX_WORDS     = 400  (ngưỡng split)
  LEGAL_MIN_WORDS     = 30   (ngưỡng merge)
  LEGAL_OVERLAP_WORDS = 50   (overlap khi fixed-word split)

Chiến lược:
  - legal   : Article-aware + Clause-aware (khoản số/chữ/bullet) + fixed-word fallback
  - forms   : 1 template = 1 chunk
  - examples: 1 ví dụ = 1 chunk

Bug đã fix so với v1:
  - CLAUSE_PATTERN v1 chỉ nhận "1. ", "2. " → bỏ sót "a) ", "b) ", "- bullet"
  - v2 thêm đầy đủ 3 dạng khoản luật VN
  - Prepend article header "[Điều X. Tên]" vào đầu mỗi chunk con để giữ ngữ cảnh

chunk_id format: {doc_id}__chunk{index:03d}
  VD: "30/2020/NĐ-CP__Điều_5.__chunk000"
  ⚠️ Lưu ý: chunk_id này KHÁC với ChromaDB ID (SHA-256 hex) — xem mục 4

--- legal_chunks ---
File    : dataset/chunks/legal_chunks.parquet  (~130 MB sau filter)
          dataset/chunks/legal_chunks.jsonl    (~520 MB sau filter)
Số chunk: ~240,000  (ước tính, tỉ lệ theo 168,946/392,079 × 593,430)
Cột     : chunk_id, doc_id, source_doc_no, ministry, type_normalized,
           doc_name, chapter_id, chapter_name, article,
           chunk_index, total_chunks, split_type, text, word_count

--- forms_chunks ---
File    : dataset/chunks/forms_chunks.parquet + .jsonl
Số chunk: 10  (1 template = 1 chunk) — không đổi
Cột     : chunk_id, doc_id, form_id, form_type, purpose, required_fields,
           chunk_index, total_chunks, split_type, text, word_count

--- examples_chunks ---
File    : dataset/chunks/examples_chunks.parquet + .jsonl
Số chunk: 150  (1 ví dụ = 1 chunk) — không đổi
Cột     : chunk_id, doc_id, example_id, form_id, form_type, scenario,
           fields_json, chunk_index, total_chunks, split_type, text, word_count


================================================================
4. VECTOR DATABASE — embedAndIndex.ipynb  ✅ HOÀN THÀNH
================================================================

Chạy trên: Kaggle Notebook, GPU Tesla T4
Input : dataset/chunks/*.parquet  (upload lên Kaggle dataset "rag-chunks")
Output: chromadb_output.zip  (tải về, giải nén thành thư mục dataset/chromadb/)

⚠️ Cần RE-EMBED sau khi filter — upload lại legal_chunks.parquet mới lên Kaggle
   trước khi chạy embedAndIndex.ipynb

Embedding model: intfloat/multilingual-e5-large-instruct
  - Dim       : 1024
  - Max seq   : 512 tokens
  - Asymmetric retrieval:
      Index (passage): prefix "passage: {text}"
      Query (search) : "Instruct: {task}\nQuery: {query}"
  - Distance  : cosine  (embeddings đã L2-normalize)
  - Batch size: 128 (T4 GPU)

ChromaDB Collections (sau filter):
  legal_chunks    → ~165,000 chunks  (~2,600 MB trên disk)  ← giảm từ 9,400 MB
  forms_chunks    → 10 chunks
  examples_chunks → 150 chunks
  Tổng: ~165,160 chunks | DB size: ~2.6 GB  ← giảm từ ~9.4 GB

⚠️ QUAN TRỌNG — ChromaDB ID vs Parquet chunk_id:
  ChromaDB lưu mỗi chunk với ID = SHA-256[:24] của:
    f"{doc_id}|{chunk_index}|{text[:200]}"
  → ID này KHÁC HOÀN TOÀN với chunk_id text-key trong parquet
  → Metadata phải lấy trực tiếp từ ChromaDB query response (include=["metadatas"])

Retrieval instructions (asymmetric):
  legal   : "Instruct: Tim dieu khoan phap luat Viet Nam lien quan.\nQuery: "
  forms   : "Instruct: Tim mau bieu hanh chinh phu hop voi nhu cau soan thao.\nQuery: "
  examples: "Instruct: Tim vi du van ban hanh chinh tuong tu tinh huong sau.\nQuery: "


================================================================
5. SCHEMA CHI TIẾT CÁC COLLECTION (ChromaDB metadata fields)
================================================================

--- legal_chunks ---
  chunk_id_raw   : str  — chunk_id text-key từ chunkData (KHÔNG phải ChromaDB ID)
  source_doc_no  : str  — số hiệu văn bản (VD: "30/2020/NĐ-CP")
  doc_name       : str  — tên văn bản đầy đủ
  type_normalized: str  — loại văn bản chuẩn hoá (NGHỊ ĐỊNH, LUẬT, PHÁP LỆNH, NGHỊ QUYẾT)
  article        : str  — tên Điều (VD: "Điều 5. Quyền và trách nhiệm...")
  ministry       : str  — cơ quan ban hành
  chapter_id     : str  — số chương
  chunk_index    : int  — thứ tự chunk trong điều
  total_chunks   : int  — tổng số chunk của điều
  split_type     : str  — "article" | "fixed_word"
  word_count     : int  — số từ trong chunk

--- forms_chunks ---
  form_id        : str  — VD: "Form_01"
  form_type      : str  — loại biểu mẫu
  purpose        : str  — mục đích sử dụng
  required_fields: str  — JSON string danh sách placeholder
  chunk_index    : int
  total_chunks   : int
  split_type     : str  — "full_template"
  word_count     : int

--- examples_chunks ---
  example_id     : str  — ID ví dụ (VD: "Form_01_ex_001")
  form_id        : str  — VD: "Form_01"
  form_type      : str  — loại biểu mẫu
  scenario       : str  — mô tả tình huống
  fields_json    : str  — JSON string các cặp field:value đã điền
  chunk_index    : int
  total_chunks   : int
  split_type     : str  — "full_example"
  word_count     : int


================================================================
6. HYBRID RETRIEVAL — hybridRetrieval_v3.ipynb  ✅ HOÀN THÀNH (v3, final)
================================================================

Pipeline v3:
  Query → [BM25 bigram top-30] ─┐
                                 ├─ RRF (k=60) → top-10 → Cross-Encoder → top-3
  Query → [Dense top-30]        ┘
                                 └─ Chunk Expand: ghép full điều luật từ parquet

Components:
  BM25     : rank_bm25.BM25Okapi | bigram tokenization, normalize không dấu
  Dense    : intfloat/multilingual-e5-large-instruct (asymmetric, 1024-dim)
  Fusion   : Reciprocal Rank Fusion (k=60)
  Reranker : BAAI/bge-reranker-v2-m3 (multilingual, max_length=512)
  Expand   : Ghép full text điều luật từ _expand_index (parquet in-memory)
  Device   : CUDA nếu có, fallback CPU

Tham số v3 (tối ưu so với v2):
  BM25_TOP_K=30  DENSE_TOP_K=30  RRF_K=60  RERANK_TOP_N=10  FINAL_TOP_K=3
  MAX_CHUNKS_PER_DOC=1  MAX_EXPAND_CHUNKS=6

Hiệu năng đo được (GPU T4):
  - retrieve_all() đầy đủ: ~4,490ms
  - Benchmark 8 queries: ~1,835–4,436ms/query
  - use_reranker=False → ~2–3s/query (dùng khi dev)

BM25 cache (v2 bigram):
  dataset/bm25/bm25_legal_v2.pkl    (~160 MB)
  dataset/bm25/bm25_forms_v2.pkl
  dataset/bm25/bm25_examples_v2.pkl

Bugs đã fix (v1→v3):
  1. _dense_search() thêm "metadatas" vào include=[] → metadata không còn rỗng
  2. meta_lookup (parquet) chỉ làm fallback cho BM25-only hits
  3. CrossEncoder(cache_folder=...) — fix deprecation
  4. get_embedding_dimension() — fix FutureWarning
  5. FORM_KEYWORD_MAP mở rộng đủ 10 form (v2 chỉ có 9, thiếu Form_01/06/08/10)
     và sửa ke_hoach/de_an nhầm sang Form_06 (Công điện) thay vì Form_04
  6. retrieve_forms() — sửa bug chỉ lấy candidates[0], bỏ qua reranker khi
     có nhiều candidates (VD: Form_02 + Form_03 cho query "quyết định")
  7. retrieve_examples() — sửa double-call detect_form_candidates()
  8. retrieve_forms() — thêm fallback khi form_id không tồn tại trong DB

FORM_KEYWORD_MAP (10 form, đầy đủ):
  Form_01: nghi_quyet
  Form_02+03: quyet_dinh (→ reranker chọn đúng)
  Form_04: to_trinh, bao_cao, chi_thi, thong_bao, huong_dan,
           ke_hoach/chuong_trinh/phuong_an/de_an/du_an,
           quy_che/quy_dinh, uy_quyen, phieu_*
  Form_05: cong_van
  Form_06: cong_dien, khan_cap, thong_bao_khan
  Form_07: giay_moi, thu_moi, moi_hop
  Form_08: giay_gioi_thieu, gioi_thieu_can_bo
  Form_09: bien_ban
  Form_10: nghi_phep, giay_nghi_phep, xin_nghi
  Fallback: None → dense search toàn bộ

Public API:
  retrieve_legal(query, top_k=3, type_filter=None, use_reranker=True, expand=True)
  retrieve_forms(query, use_reranker=True)
  retrieve_examples(query, top_k=3, form_id=None, use_reranker=True)
  retrieve_all(query, legal_top_k=3, examples_top_k=3,
               legal_type_filter=None, expand_legal=True)
    → {"legal": [...], "form": [...], "examples": [...]}

Module tái sử dụng:
  hybrid_retrieval.py  ← import được vào ragGeneration, FastAPI, v.v.
    from hybrid_retrieval import init_retriever, retrieve_all
    init_retriever()   # gọi 1 lần khi start
    results = retrieve_all(query)

Filter hợp lệ:
  type_normalized: "LUẬT" | "NGHỊ ĐỊNH" | "PHÁP LỆNH" | "NGHỊ QUYẾT"
  (THÔNG TƯ và QUYẾT ĐỊNH đã bị loại khỏi dataset)


================================================================
7. CẤU TRÚC THƯ MỤC LOCAL (hiện tại)
================================================================

project/
├── dataset/
│   ├── raw/
│   │   ├── legal_dataset.parquet
│   │   ├── forms_dataset.parquet
│   │   └── forms_examples_dataset.parquet
│   ├── processed/
│   │   ├── legal_dataset_processed.parquet    (392,079 hàng — output preprocessData)
│   │   ├── legal_dataset_filtered.parquet     (~110,000 hàng — output filterData) ← mới
│   │   ├── forms_dataset_processed.parquet    (10 hàng)
│   │   └── forms_examples_dataset_processed.parquet  (150 hàng)
│   ├── chunks/
│   │   ├── legal_chunks.parquet    (~240,000 chunks, ~130 MB)  ← giảm từ 593k/327MB
│   │   ├── legal_chunks.jsonl      (~520 MB)                   ← giảm từ 1,336 MB
│   │   ├── forms_chunks.parquet    (10 chunks)
│   │   ├── forms_chunks.jsonl
│   │   ├── examples_chunks.parquet (150 chunks)
│   │   └── examples_chunks.jsonl
│   ├── chromadb/                  ← giải nén từ chromadb_output.zip (~2.6 GB)
│   │   └── chroma.sqlite3 + ...
│   └── bm25/                      ← tạo tự động khi chạy hybridRetrieval
│       ├── bm25_legal_v2.pkl      (~160 MB, bigram)
│       ├── bm25_legal_v2.meta.pkl
│       ├── bm25_forms_v2.pkl
│       ├── bm25_forms_v2.meta.pkl
│       ├── bm25_examples_v2.pkl
│       └── bm25_examples_v2.meta.pkl
├── models/
│   ├── embedding/   ← cache intfloat/multilingual-e5-large-instruct (~1.1 GB)
│   └── reranker/    ← cache BAAI/bge-reranker-v2-m3 (~1.1 GB)
├── checkData.ipynb
├── preprocessData.ipynb
├── filterData.ipynb               ← mới thêm vào pipeline
├── chunkData.ipynb                ← đã cập nhật input path
├── embedAndIndex.ipynb            ← Kaggle: build ChromaDB (cần re-run)
├── hybridRetrieval_v3.ipynb        ← Local: BM25 bigram + Dense + Expand (final)
└── hybrid_retrieval.py                ← Module tái sử dụng cho RAG pipeline


================================================================
8. ƯỚC TÍNH DUNG LƯỢNG TRƯỚC/SAU FILTER
================================================================

| Thành phần          | Trước filter | Sau filter   |
|---------------------|-------------|--------------|
| legal_chunks.parquet| 327 MB      | ~130 MB      |
| legal_chunks.jsonl  | 1,336 MB    | ~520 MB      |
| ChromaDB            | ~9,400 MB   | ~3,700 MB    |
| BM25 pkl            | ~400 MB     | ~160 MB      |
| Models (embed+rerank)| 2,200 MB   | 2,200 MB     |
| **Tổng**            | **~13.7 GB**| **~6.7 GB**  |

Lưu ý: models không đổi. Nếu xóa legal_chunks.jsonl thì xuống ~6.2 GB.
⚠️  Dung lượng thực tế có thể chênh lệch — cập nhật lại sau khi re-embed xong.


================================================================
9. HƯỚNG DẪN RE-EMBED TRÊN KAGGLE
================================================================

Sau khi chạy filterData.ipynb và chunkData.ipynb local:

1. Upload file mới lên Kaggle dataset "rag-chunks":
   - legal_chunks.parquet  (phiên bản mới ~130 MB)
   - forms_chunks.parquet  (không đổi)
   - examples_chunks.parquet (không đổi)

2. Mở embedAndIndex.ipynb trên Kaggle, đặt:
   RESET_DB = True   ← xóa ChromaDB cũ, build lại từ đầu

3. Chạy toàn bộ notebook (~45 phút thay vì ~3.5 giờ)

4. Tải chromadb_output.zip về, giải nén vào dataset/chromadb/

5. Xóa bm25_legal.pkl cũ (nếu có):
   del dataset/bm25/bm25_legal_v2.pkl
   del dataset/bm25/bm25_legal_v2.meta.pkl
   → hybridRetrieval_v3 sẽ tự build lại khi chạy lần đầu


================================================================
10. BƯỚC TIẾP THEO
================================================================

  □ RAG Generation Pipeline (ragGeneration.ipynb):
      - Input: user_query (mô tả nhu cầu soạn thảo)
      - Gọi retrieve_all(query) → legal + forms + examples context
      - Ghép context theo template prompt
      - Gọi LLM API → sinh văn bản hành chính
      - Xử lý output: điền placeholder, format markdown

  □ Chọn LLM generation model:
      - GPT-4o / GPT-4o-mini (OpenAI API) — khuyến nghị trước
      - Gemini 1.5 Pro (Google API)
      - Local: Qwen2.5 / Vistral (nếu cần offline)

  □ Prompt templates (promptTemplates.py):
      - System prompt chung (vai trò chuyên viên hành chính VN)
      - User prompt theo từng loại văn bản (công văn, quyết định, nghị quyết...)
      - Format context block: legal_context + form_template + example_context
      - Hướng dẫn điền placeholder {{FIELD}} cho LLM

  □ Evaluation:
      - Retrieval: MRR@5, Recall@5 trên tập test query thủ công
      - Generation: ROUGE, BERTScore, đánh giá thủ công format & nội dung

  □ (Tuỳ chọn) API / UI:
      - FastAPI endpoint: POST /generate {query, form_type?, filters?}
      - Gradio UI cho demo

================================================================